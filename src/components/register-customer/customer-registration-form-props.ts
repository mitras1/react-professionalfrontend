import { CustomerProfile } from "../../models";

interface CustomerRegistrationFormProps {
    customerProfile?: CustomerProfile;
    registrationStatus?: boolean;
}

export default CustomerRegistrationFormProps;
